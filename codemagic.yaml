workflows:
  android-release:
    name: Android APK Build
    environment:
      flutter: 3.19.6
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk