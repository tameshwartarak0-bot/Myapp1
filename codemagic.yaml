workflows:
  android-release:
    name: Android APK Build
    working_directory: .
    environment:
      flutter: 3.3.4
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk