workflows:
  android-release:
    name: Android APK
    instance_type: mac_mini_m1
    environment:
      flutter: stable
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk